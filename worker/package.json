{
  "name": "contact-form-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for portfolio contact form",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
